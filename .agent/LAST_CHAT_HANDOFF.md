# Last Chat Handoff

Memory layer created from a read-only audit. `AGENTS.md`, core `.agent` files, runbooks, and workflow skill notes were filled automatically.

Confirmed: Flutter/Dart app, Riverpod state management, Android package/app label defaults, Mihomo v1.19.25 from `core/go.mod`, Android VPN remote service architecture, runtime config sanitizer, subscription header parser, Remnawave/HWID request metadata path, generated code directories, and tag-based GitHub release workflow.

Needs manual/live verification: local Flutter version, real Android VPN behavior, Remnawave panel compatibility, FFI generation path, signing ownership, official release artifacts, and full release reproduction.

Next recommended steps: run `flutter --version`, `flutter pub get`, `flutter test`, and inspect whether `pubspec.yaml` ffigen paths are stale before any core/FFI changes.
